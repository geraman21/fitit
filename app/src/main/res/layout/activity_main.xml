<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:theme="@style/AppTheme"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <ImageButton
            android:layout_width="413dp"
            android:layout_height="0dp" app:srcCompat="@drawable/fablab"
            android:id="@+id/fabLabButton"
            android:scaleType="centerCrop"
            android:layout_marginBottom="8dp" app:layout_constraintBottom_toTopOf="@+id/mensaButton"
            app:layout_constraintTop_toBottomOf="@+id/libraryButton"/>
    <ImageButton
            android:layout_width="388dp"
            android:layout_height="249dp" app:srcCompat="@drawable/library2"
            android:id="@+id/libraryButton"
            android:scaleType="centerCrop"

            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" app:layout_constraintEnd_toEndOf="parent"
    />
    <ImageButton
            android:layout_width="413dp"
            android:layout_height="249dp" app:srcCompat="@drawable/mensa"
            android:id="@+id/mensaButton"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" android:layout_marginBottom="16dp"/>
</android.support.constraint.ConstraintLayout>